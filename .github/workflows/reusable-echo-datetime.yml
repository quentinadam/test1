name: Reusable Echo Datetime

on:
  workflow_call:

jobs:
  run-composite-action:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run composite action
        uses: ./.github/actions/echo-datetime
